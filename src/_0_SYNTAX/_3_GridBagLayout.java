package _0_SYNTAX;

public class _3_GridBagLayout {
}

/*

    gbc.gridy = row (0-indexed)
    gbc.gridx = column (0-indexed)

    gbc.insets = new Insets(5, 10, 5, 10); // top, left, bottom, right

    Use gbc.gridwidth = 2 to merge columns:
        gbc.gridheight to merge height

 */
